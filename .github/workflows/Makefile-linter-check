name: Makefile-linter-check

on:
  - push
  - pull_request

jobs:
  build:

    runs-on: macos-latest

    strategy:

      matrix:
        node-version: [13.x]

    steps:

      - uses: actions/checkout@v2

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - name: npm install, build
<<<<<<< HEAD
        run: make install
        env:
          CI: true
      - name: Run linter
        run: make lint
=======
        run: |
          make install
        env:
          CI: true
      - name: Run linter
        run: |
          make lint
>>>>>>> d8743a7... edited readme
          
